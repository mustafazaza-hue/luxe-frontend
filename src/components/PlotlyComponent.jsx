'use client';

import { useEffect } from 'react';
import Plotly from 'plotly.js-dist-min';

export default function PlotlyComponent() {
  useEffect(() => {
    // يمكنك إضافة منطق Plotly هنا إذا أردت
    return () => {
      // تنظيف
    };
  }, []);

  return null;
}